# odd-even-checker
python program to check if a number is odd or even using user input.
num = int(input("enter a number"))
if num % 2 == 0:
    print(f"{num}number is even")
else:
    print(f"{num}number is odd")
